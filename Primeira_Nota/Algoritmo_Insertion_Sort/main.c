#include "exercicio1.h"

/*
	Running the program, using MakeFile, through the Linux Operating System Terminal.
		1 - Open the Linux Terminal. When opening, it is in the folder, where the files of this program are.
		2 - Now, type make all followed by ENTER. It will generate the compilation of the file(s).
		3 - Then, type make run followed by ENTER. This causes the program to run.

	Executando o programa, usando MakeFile, por meio do Terminal do Sistema Operacional Linux.
		1 - Abra o Terminal do Linux. Ao abrir, esteja na pasta, onde se encontram os arquivos
		deste programa. 
		2 - Agora, digite make all   seguido de ENTER. Vai gerar a compilação do(s) arquivo(s).
		3 - Depois, digite make run  seguido de ENTER. Isso faz com que o programa seja executado.
*/

int main(void){
	inicio1();
	inicio2();
	return 0;
}